// Emulator-only. Production Firestore access is via firebase-admin from
// Cloud Run, which bypasses rules; client-direct reads/writes are not used.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

// Emulator-only. Production GCS access is via the admin Storage client from
// Cloud Run; clients never upload directly.
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
